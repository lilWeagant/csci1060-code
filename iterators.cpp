#include <iostream>
#include <vector>

using namespace std;

/*
int findMax(vector<int> v){

    int max = v[0];
    for (int i = 0; i < v.size(); i++){
        if (max < v[i]){
            max = v[i];
        }
    }
    return max;

}
*/
int main(){

    
    /*
    //Find the max value in a vector
    vector<int> v = {5, 8, 2, 8, 5, 8, 2};
    cout << "The max value is: " << findMax(v) << endl;
    */
    /*
    //Iterator Examples
    vector<int> v = {3, 4, 5}; //define vector
    vector<int>::iterator it; //define iterator

    for (it = v.begin(); it != v.end(); it++){
        cout << *it;
    }
    cout << endl;

    vector<int>::reverse_iterator itr; //define reverse iterator

    for (itr = v.rbegin(); itr != v.rend(); itr++){
        cout << *itr;
    }
    cout << endl;
    */

    return 0;
}